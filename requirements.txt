# Frozen inference environment (Team Fluxx, CUHK-X Large Model Track).
# Python 3.11. CPU-only; no CUDA needed (solver pins torch to CPU/MPS).
# Install: pip install -r requirements.txt
--extra-index-url https://download.pytorch.org/whl/cpu
numpy==1.26.4
pandas==3.0.5
scikit-learn==1.9.1
scipy==1.17.1
torch==2.11.0+cpu
